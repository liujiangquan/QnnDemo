# 此目录用于存放 arm64-v8a 预编译 .so 文件。
# 运行 docs/setup_demo.ps1 会自动从 QAIRT SDK 拷贝以下库到此处：
#   - libQairtSystem.so  (必需)
#   - libQairtCpu.so     (必需, CPU 后端)
#   - libQairtGpu.so     (可选, GPU 后端)
#   - libQairtHtp.so     (可选, HTP/NPU 后端)
