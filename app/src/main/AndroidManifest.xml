<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- aosp 分支：走 unsigned PD 路线，不需要 android.uid.system。
         去掉 sharedUserId 让 app 装成普通 uid，可以绕开 Meizu Flyme 的
         allowList 加固。QNN 通过 APK 内 jniLibs + assets/htp/ 自带的
         unsigned skel 走 unsigned PD，不依赖 vendor QNN 集成。 -->

    <!-- 读取 DLC 模型文件（如从外部存储加载时） -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QnnDemo">

        <!-- FastRPC 用户态通道（stub 内部会 dlopen 这两个 vendor 库跟 DSP 通信） -->
        <uses-native-library
            android:name="libadsprpc.so"
            android:required="false" />
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
